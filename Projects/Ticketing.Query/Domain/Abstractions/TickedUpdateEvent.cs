namespace Tiketing.Query.Domain.Abstractions
{
  // NOTA: Esta clase es un placeholder/stub local que aún no está implementado.
  // Su propósito final sería representar un evento de actualización de ticket
  // específico para el dominio del Query Side, si fuera necesario diferenciarlo
  // del evento compartido (Common.Core.Events.TicketUpdatedEvent).
  //
  // Por ahora está vacía — el Query Side usa directamente el evento del Common.Core.
  public class TickedUpdateEvent
  {
  }
}